<div class="posts">

    {{#each posts}}

    <section>
        <div class="card">
            <div class="card-body">
                <h1><a href="/post/{{id}}">{{title}}</a></h1>
                <p>{{body}}</p>
            </div>
        </div>
    </section>

    {{/each}}

</div>